import 'mocha'

describe('remove actions', () => {
    describe('overflow menu', () => {
        it('displays a confirmation before removing') // TODO
        it('does not remove if not confirmed') // TODO
        it('removes the term after confirming') // TODO
    })
});